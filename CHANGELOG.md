# 变更日志（Changelog）

> 用户可见变化；开发流水与调试案例见 `AnkeShelf_DevLog.md` 与
> `docs/DEVLOG_ARCHIVE.md`。版本线分离：Windows `vX.Y.Z`，Android `android-vX.Y.Z`。

## Windows

### v1.7.0（2026-08-25）
- 新增 **楼层导出**：按楼层导出 PNG/WebP，支持 NGA / 骨碌碌、多选、主题（浅色/羊皮纸/深色/按当前阅读设定）、倍率、进度条、目录选择；阅读器楼层头一键分享当前楼层。
- 新增 **GitHub 版本更新提醒**：进入应用时礼貌提醒；网络失败静默。
- NGA 外链音乐与站内附件音频支持在线播放，标题显示为可读文件名，自动播放可开关，音乐控制入口补齐。
- 安全加固：API 分发收口、NGA 图床主机名精确匹配、明文 http 拒绝、依赖审计。

### v1.6.2（2026-08-22）

- 修复 NGA 楼层卡片在旧 WebView 内核下边框与卡底丢失的问题：内联样式恢复浅/深主题实色兜底，现代内核继续使用 color-mix 自适应主题。
- 修复 Windows 端 `.nga-floor` 背景使用 `--reader-bg` 导致卡底视觉上不可见的问题，改为与引用块一致的 `--reader-fg 6%`。

### v1.6.1（2026-08-22）

- 阅读器性能：翻页 / 定位 / 滚动的进度采样减半（分页模式大章节翻页更跟手）；空白页跳过判定提前退出并按布局缓存，重访页零成本。
- 进度与标注的保存失败不再静默丢失：本地服务异常时弹出提示，标注 / 书签操作失败保留现场可重试。
- 内置霞鹜文楷字体改为 WOFF2 无损压缩（26MB → 10MB），安装包与内存占用显著下降；旧字体设置自动兼容。

### v1.6.0（2026-08-21）

- 标签系统：可给书籍设定多个标签（内容 + 颜色，名称 ≤10 字），下载的安科默认带 NGA / 骨碌碌标签；书架与书籍管理支持按标签筛选和搜索。
- NGA 网页登录后自动保存有效登录配置（直接退出 / 点提取 / 点取消，只要拿到有效凭据即保存）。
- 骨碌碌导入支持“清除本地缓存后重下”。
- 阅读器：折叠块摘要修复（双端）、骰子灰遮罩修复、折叠 / 链接 / 按钮点击不再误触悬浮栏。

### v1.5.1（2026-08-20）

- 修复 Windows 窗口版发行包中 NGA 图片代理调用系统 curl 子进程导致加载时弹出大量终端窗口、速度变慢的问题；改为使用 curl_cffi 浏览器指纹抓取图片。

### v1.5.0（2026-08-20）

- NGA 楼层 / 引用 / 评论框改为随阅读器主题自适应，下载时不再需要选择深色 / 浅色主题。
- NGA 官方表情图改为直连，加载失败时自动降级为文字表情，不再显示裂图占位。
- 骨碌碌封面本地缓存：导入 / 热更新时自动获取网络封面并缓存，热更新即使无新增楼层也会同步重载封面。
- 书架封面管理：新增“更多管理”二级菜单（重命名 / 设置封面 / 恢复默认封面 / 移除），网格封面不再按钮超界。
- 默认封面统一为平面骰子占位，随当前主题 / 色板自适应颜色。
- NGA / 骨碌碌标签在网格、列表、最近阅读中统一显示在封面下方。
- 设置页新增“书籍管理”页签：支持搜索、导出、重命名、封面与移除。
- 下载 / 更新完成后立即刷新书架；顶部最近阅读栏补充作者与进度。
- 隐藏书名前缀支持连续剥离 `【...】` 与 `[...]`。
- 修复 NGA 图片代理在部分网络下被 567 拦截的问题：优先使用系统 curl 拉取图片。

### v1.4.0（2026-08-16）

- 骨碌碌阅读交互全面改造：沉浸功能（音乐/背景/视效）由侧边卡片改为**右下角悬浮气泡**；
  评论面板改为**侧边展开**（正文让位，与阅读界面同层级）；新增**段落级评论**（正文段落
  评论徽标 ↔ 面板段落分组联动，点击双向跳转并高亮）。
- 新增**沉浸内容总览**：按楼层 / 按类型双视图，展示音乐（歌名+楼层）、氛围背景预览、
  动态视效、骰点解锁进度；活跃中内容高亮并实时刷新。
- 骰点解锁：一级入口气泡菜单（解锁下一组 / 解锁本章全部）；重置拆分为本章 / 全书
  （骰点与秘密线索分别可重置）。
- 音乐增强：顶栏播放气泡（歌名+楼层）、进度条与播放/暂停键；背景切换淡入过渡。
- 修复：评论跳转/抽屉开合进度漂移（跨 iframe 坐标与空白节点定位）、滚动模式底部
  内容裁剪、折叠默认展开、骰点/评论在迷雾中的可见性与跳转。

### v1.3.1（2026-08-15）

- 骨碌碌专版阅读页新增目录、评论、书签、音乐与氛围、骰点揭示和阅读设置一级快捷入口；
  更多菜单仅保留全屏和单书阅读解锁重置。
- 对齐全能助手 V3.94 Reader：支持骰点遮罩、按楼迷雾、点击音效、连续十组揭示、自动
  音乐、背景与视效，并补齐文本引用的同书楼层跳转和跨书公开链接。
- 评论按当前章全部楼层在线加载，可在侧边面板或楼末折叠显示，不计入正文坐标；新增
  章节加载遮罩、骨碌碌封面标签，并保持 NGA 阅读界面隔离。
- 修复分页模式进入/退出沉浸阅读时进度漂移，以及宽版正文滚动锚点采样为零的问题。

### v1.3.0（2026-08-15）

- 新增骨碌碌公开书籍导入：支持书籍 ID / 链接、站点目录分章，以及未设置章节作品按楼层
  自动回退分章；导入过程可取消并显示进度。
- 评论默认在线加载并带 5 分钟缓存与离线回退，不增加书籍体积；需要跨阅读器查看时可
  单独导出含评论 EPUB。阅读器支持只读弹幕与子回复展示，不改变正文阅读坐标。
- 适配骨碌碌全能助手内容：文本折叠、线索与 AES 秘密，以及音乐、氛围背景、雨雪风雷
  和震动效果；自动音乐默认关闭，背景与视效可独立停用。
- 正文图片支持在线、内嵌与不含三种模式；已导入作品可检查更新，仅下载末尾新增楼层，
  远端历史被删除、重排或替换时明确要求完整重导，更新保持书籍 ID、进度与标注。
- 修复分页边缘露出下一页文字、过长单行溢出，以及进入沉浸阅读后退出会回到章节首并
  丢失原窗口状态的问题。
- 加强发行稳定性：依赖锁定、契约与 API 漂移守卫、备份/存储损坏恢复、诊断导出和
  Windows CI 多版本测试与目录版打包。

### v1.2.0（2026-08-07）

- 独立全文检索页：按章分组折叠、每章限量 + 续取、大小写敏感/全词匹配、每书搜索历史；
  高频词按章限量返回，靠后章节不被挤掉。
- 个性化主题：9 套预设色板 + 自定义背景/主题/强调/文字四色（仅作用于默认黑/白文字，
  NGA 彩色字保留）；支持跟随系统深浅色。
- 设置页重构：外观 / 阅读 / 辅助 / 快捷键 / 统计 / 数据分栏；下载 / 导出整合页四页签。
- 阅读统计默认汇总全部书目，可进入详情按书查看。

### v1.1.0（2026-08-06）

- 滚动阅读改为默认（旧设置一次性迁移），滚动语义“一章到底、不分页”；单/双翻页重构，
  对齐 flow/epub.js 列几何，双页自动补偶数列；顶/底栏自动隐藏重写。
- NGA 下载与热更新：首次下载即建原生书，热更新纯增量；独立“更新帖子”面板（仅对新增
  楼层生效，默认保留上次设置）；导出文件名默认使用安科标题。
- 自定义背景/主题/强调/文字颜色；沉浸式全屏阅读（F11）。

### v1.0.0（2026-08-06）

- NGA 帖子一站式下载（Cookie 配置、只看楼主、楼层范围、图片嵌入/在线）→ 本地 EPUB，
  完整还原楼层卡片、引用、骰子、彩色字与长表格页内滚动。
- 连载热更新：只增量拉取新楼层，进度与标注保持稳定。
- 滚动/分页/自动双页/强制横屏双页；全文搜索、标注（高亮/笔记/书签）、阅读统计、
  自定义字体与排版、快捷键、图片点击放大、最近阅读横条、网格/列表书架。
- 旧版 `%APPDATA%\EpubReader` 数据首次启动自动迁移。

## Android

### android-v1.4.0（2026-08-25）
- 新增 **楼层导出**（对齐 Windows）：NGA / 骨碌碌楼层多选、精确筛选、最新在前、主题/PNG/WebP/倍率、批量导出、分享本批；阅读器楼层头一键分享。
- 新增 **GitHub 版本更新提醒**：进入应用时礼貌弹窗；网络失败静默。
- 修复华为 WebView 上 NGA 楼层卡片消失；修复 NGA 内嵌图片下载无进度/卡死。
- NGA 外链音乐与站内附件音频支持在线播放；音乐控制入口补齐；自动播放开关。
- 长按图片放大支持双指缩放与拖动；骨碌碌头像模式放大。
- 修复阅读页 `file://` 子资源被拦截导致图片/音乐卡片不显示的问题。

### android-v1.3.2（2026-08-22）

- 修复 NGA 楼层卡片在 Android 10 等旧 WebView 上只剩蓝色左线、灰边与卡底丢失的问题。
- 与 Windows v1.6.2 楼层卡片样式对齐。

### android-v1.3.1（2026-08-22）

- 性能优化：翻页与重排的进度采样减半、空白页判定提前退出并按布局缓存，翻页更跟手。
- 内置霞鹜文楷字体改为 WOFF2 无损压缩（26MB → 10MB），安装包缩小约 16MB，进书加载更快。
- 本地数据文件损坏 / 读取失败时书架显示醒目横幅（不再静默清空）；读取失败期间暂停写入，保护原文件不被空数据覆盖。
- 修复跨章跳转（标注 / 书签）回到初始章时被旧滚动位置覆盖的问题。

### android-v1.3.0（2026-08-21）

- 对应 Windows v1.6.0：标签系统、NGA 登录自动保存、阅读设置卡片与登录配置排版优化、
  全文检索键入书名筛选、骨碌碌清除缓存后重下、折叠块摘要与骰子遮罩修复。

### android-v1.2.0（2026-08-20）

对齐 Windows 端功能的大版本（分 10 批实施，详见 `docs/ANDROID_PARITY_PLAN.md`）。

阅读器
- 新增标注交互：长按选中文字即可高亮（6 色）、加笔记、删除；点击高亮可改色/编辑/删除。
- 新增书签：顶栏一键添加/删除当前位置书签；新增“标注与书签”抽屉，点击可跳转（支持跨章）。
- 目录支持 EPUB 多级层级（缩进显示、当前章高亮、打开即滚到当前章）。
- 底栏新增全书进度滑块（松手才跳转）。
- 新增阅读辅助面板：自动滚动（滚动/分页两种模式，到章尾自动进入下一章）、速读（RSVP，
  中文按词块闪现）、可拖动阅读标尺、按本书选择字体。
- 代码块语法高亮。
- 书架进度百分比改为“章号 + 章内比例”，不再整章取整。

骨碌碌（全新支持）
- 下载页新增“骨碌碌导入”：粘贴公开链接或书籍 ID 即可转成标准 EPUB 并入架，
  支持图片在线 / 内嵌 / 不含三态，前台服务带进度与取消。
- 支持增量热更新：只拉新增楼层；远端删楼/重排会明确拒绝并提示完整重新导入。
- 阅读时支持全能助手与沉浸内容：折叠、引用跳转、骰点揭示（单组 / 整楼 / 接下来 10 组）、
  迷雾、秘密与线索、音乐（含自动音乐）、氛围背景、视效。
- 在线评论：按楼按需加载，支持段落评论徽标联动与只读弹幕；离线时回退最近缓存并提示。
- 新增“骨碌碌总览”：本章元素统计、批量揭示、重置解锁（不影响阅读进度与书签）。

NGA
- 下载参数补齐：本次最多下载页数、目录楼 pid、按目录楼分章（与桌面产物同构）。

数据与设置
- 设置页新增“校验数据完整性”，逐个检查五个 JSON 存储能否解析与版本字段。
- 修复与 Windows 共用 `settings.json` 时会丢失骨碌碌沉浸偏好的问题。
- 书架新增按作者排序。

### android-v1.1.0（2026-08-20）

- NGA 楼层 / 引用 / 评论框随阅读器主题自适应，下载时移除深色 / 浅色主题选择。
- 新增“书籍管理”入口：搜索、导出、重命名、设置封面、恢复默认封面、移除。
- 书架封面管理：网格封面保留更新按钮，其余操作收进“更多管理”二级菜单。
- 骨碌碌 / NGA 默认封面统一为骰子占位，并随主题自适应颜色。
- NGA / 骨碌碌标签统一显示在封面下方。
- 隐藏书名前缀支持连续剥离 `【...】` 与 `[...]`。
- 修复骨碌碌封面缓存同步可能清空已有封面缓存的问题。

### android-v1.0.0（2026-08-09）

- 对齐 Windows v1.2.0 功能清单：书架（网格/列表、排序、重命名/删除）、导入 EPUB、
  NGA 下载与增量追更（前台服务 + 通知 + 取消清理）、导出 EPUB/Markdown（SAF）。
- 阅读：分页（横屏自动双页）/滚动、NGA 排版完整还原、深色/浅色/羊皮纸主题、
  内置霞鹜文楷与自定义字体导入、沉浸式隐藏系统栏。
- 进度精确保存：分页页码 + 段落、滚动中线文本锚点（全图页按滚动比例近似）、
  换章/退出/退后台立即落盘。
- 全文检索（每章限量续取）、6 色高亮/笔记/书签/导出、阅读统计、设置六菜单、
  内置使用说明；`allowBackup=false`，凭据仅存私有目录。
- 发行资产于 2026-08-10 以正式 Logo 重新打包。
